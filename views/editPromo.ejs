<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Announcer · Edit Promo Data</title>
        <link
            rel="icon"
            href="../public/img/psn-logo.jpeg"
            type="image/x-icon" />
        <link
            rel="stylesheet"
            href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
        <link
            rel="stylesheet"
            type="text/css"
            href="../public/style/style.css" />
    </head>
    <body>
        <div class="container mt-5">
            <h1>Edit Promo</h1>
            <form method="post" action="/edit/promo/<%= item.id %>">
                <div class="form-group">
                    <label for="promo">Promo</label>
                    <textarea
                        type="text"
                        class="form-control"
                        id="promo"
                        name="promo"
                        style="width: 500px;"
                        rows="3"
                        required><%= item.promo %></textarea>
                </div>
                <div class="form-group">
                    <label for="detail">Detail</label>
                    <textarea
                        type="text"
                        class="form-control"
                        id="detail"
                        name="detail"
                        style="width: 500px;"
                        rows="6"
                        required><%= item.detail %></textarea>
                </div>
                
                <div class="form-group">
                    <label for="awal_promo">Awal Promo</label>
                    <input
                        type="date"
                        class="form-control"
                        id="awal_promo"
                        name="awal_promo"
                        style="width: 170px"
                        value="<%= item.awal_promo.toISOString().substring(0, 10) %>"
                        required />
                </div>
                <div class="form-group">
                    <label for="akhir_promo">Akhir Promo</label>
                    <input
                        type="date"
                        class="form-control"
                        id="akhir_promo"
                        name="akhir_promo"
                        style="width: 170px"
                        value="<%= item.akhir_promo.toISOString().substring(0, 10) %>"
                        required />
                </div>
                <a href="/announcer/index" class="btn btn-secondary mt-2">Kembali</a>
                <button type="submit" class="btn btn-primary mt-2">
                    Simpan
                </button></a>
            </form>
        </div>
    </body>
</html>
